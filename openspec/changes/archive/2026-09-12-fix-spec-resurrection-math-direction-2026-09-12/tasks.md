## 1. Spec Text Correction

- [x] 1.1 Replace the L248 paragraph in `openspec/specs/decompmoe-skeleton/spec.md` Scenario `should_resurrect semantic interpretation` so that "`flag_step ⟹ flag_avg` is **not** universally true" is rewritten to "`flag_step ⟹ flag_avg` **IS** universally true (proved by elementary algebra: ∀j: H[j][i] < T ⇒ Σ H[j][i] < consec·T ⇒ mean < T)"; and the phrase "they are not nested by inclusion" is rewritten to "per-step ⊊ avg-window (per-step triggers only on histories that also trigger avg-window, but not conversely)"; and delete the contradictory qualifier "only when the avg-window condition holds on the same history". Verify by reading the edited L248 region and confirming: (a) the algebraic proof appears verbatim, (b) "⊊" symbol appears in the new paragraph, (c) the self-contradictory "only when the avg-window condition holds on the same history" qualifier is gone, (d) `git diff openspec/specs/decompmoe-skeleton/spec.md` shows ONLY this paragraph changed in the math-derivation block.
- [x] 1.2 Replace `src/decompmoe/beta.py:46` with `src/decompmoe/beta.py:50` at the line referencing `MAX_GRAD_PER_GAMMA_PHASE4: Final[float] = 0.5 * 31.0` in `openspec/specs/decompmoe-skeleton/spec.md` Requirement "Beta Parameterization Operational Domain — D1 Module-Level Constants" (currently around L423). Verify by `grep -n "beta.py:50" openspec/specs/decompmoe-skeleton/spec.md` showing exactly one hit inside the Beta Parameterization Operational Domain requirement, AND `sed -n '50p' src/decompmoe/beta.py` returning `MAX_GRAD_PER_GAMMA_PHASE4: Final[float] = 0.5 * 31.0  # = 7.75·2 = 15.5` (line-of-truth confirmation per project's spec↔code reference rule).

## 2. Test Function Name Correction

- [x] 2.1 Rename the function at `tests/test_safeguards.py:677` from `test_should_resurrect_per_step_is_strict_superset_of_avg_window_for_monotonic_history` to `test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` (replace `strict_superset` with `strict_subset`; do NOT change any other token in the function name). Verify by `grep -n "def test_should_resurrect_per_step_is_strict_subset" tests/test_safeguards.py` returning the renamed function with the new name, AND `grep -n "strict_superset" tests/test_safeguards.py` returning ZERO hits.

## 3. Verification

- [x] 3.1 Run `uv run pytest tests/test_safeguards.py -v` and verify all tests pass, including the renamed `test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` and the unchanged `test_should_resurrect_current_per_step_semantic_pinned`. The three sub-assertions of the renamed test must still hold: (a) constant history `[0.005]*200` → all 16 experts flagged; (b) Counterexample A `[0.005]*199 + [0.99]` → empty set returned by `should_resurrect` (per-step NO TRIGGER while avg-window would TRIGGER with mean 0.009925); (c) Counterexample B `[0.05]*199 + [0.005]` → empty set (both readings agree NO TRIGGER, mean 0.049775 > threshold).
- [x] 3.2 Run `python scripts/lint_no_dead_defensive.py` and verify `exit=0`. This gate enforces no dead defensive code was introduced by the spec text edits.
- [x] 3.3 Run `python scripts/lint_no_source_field_drift.py` and verify `exit=0`. This gate enforces that every spec Requirement's `**Source:**` field contains a `wayfinder/tickets/<ID>.md` literal back-reference (decompmoe-skeleton capability rule). Both modified requirements already cite `wayfinder/tickets/A6a-2.md` (Five Numerical Safeguard Helpers) and `change fix-openspec-doc-bugs design.md (Decision 7)` — neither reference is touched by this change.
- [x] 3.4 Independent numerical verification (per CLAUDE.md §3 "Post-archive independent review" protocol): hand-compute the three closed-form values that the corrected spec asserts, and confirm they hold exactly: (a) `flag_step ⟹ flag_avg`: substitute the algebra proof chain `∀j: H[j][i] < T` ⇒ `Σ H[j][i] < consec · T` ⇒ `(1/consec) · Σ H[j][i] < T` and confirm each implication is valid (transitivity of < on real numbers, division by positive constant preserves inequality); (b) Counterexample A avg-window mean `(199·0.005 + 1·0.99)/200 = 1.985/200 = 0.009925`, verified `pytest.approx(0.009925, abs=1e-6)`; (c) Counterexample B avg-window mean `(199·0.05 + 1·0.005)/200 = 9.955/200 = 0.049775`, verified `pytest.approx(0.049775, abs=1e-6)`. Write the verification outcome (numbers + verdict) into a one-paragraph note attached to the archive PR description.

## 4. Archive

- [x] 4.1 Run `/opsx:archive` (or `openspec archive --change fix-spec-resurrection-math-direction-2026-09-12`) only after tasks 3.1, 3.2, 3.3, 3.4, 5.1, 5.2 all complete successfully. The archive step must (a) sync the spec delta into `openspec/specs/decompmoe-skeleton/spec.md` (replacing the L248 paragraph and updating L423 line reference), (b) move the change directory under `openspec/changes/archive/`, (c) NOT touch any `src/decompmoe/` source file. Verify by `git status` showing only `openspec/specs/decompmoe-skeleton/spec.md`, `tests/test_safeguards.py` (the Sub-assertion 4/5 additions), and `openspec/changes/archive/fix-spec-resurrection-math-direction-2026-09-12/...` as modified/added paths — no `src/` files in the diff.

## 5. TDD Math Constraints — Review Follow-up (Gap A + B)

> Origin: post-apply comprehensive review (`/code-review` workflow) identified two low-severity gaps in the test's mathematical principle coverage. Both gaps are pure test additions (no source/spec change); the existing 3 sub-assertions remain intact.

- [x] 5.1 Add Sub-assertion 4 (universal-direction positive example on non-constant H) to `tests/test_safeguards.py::test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history`. The new sub-assertion constructs `H = [0.001]*199 + [0.030]` (every snapshot has every f_i strictly < threshold 1/32 = 0.03125), asserts the avg-window closed-form mean `(199·0.001 + 1·0.030)/200 = 0.229/200 = 0.001145` via `pytest.approx(0.001145, abs=1e-6)`, and asserts `should_resurrect(H, …) == set(range(N_e))` (all 16 experts flagged). Update the test docstring to mention Sub-assertion 4 in the closing summary and add a brief description of the universal-direction positive example. Verify by `grep -n "Sub-assertion 4" tests/test_safeguards.py` showing the description block, AND `grep -n "H_C" tests/test_safeguards.py` showing the variable definition, AND `uv run pytest tests/test_safeguards.py::test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history -v` PASSING.
- [x] 5.2 Add Sub-assertion 5 (strict less-than boundary) to the same test function. The new sub-assertion constructs `H = [1/32]*250` (every snapshot has every f_i EXACTLY at threshold 1/32 = 0.03125), asserts `avg_mean_boundary == pytest.approx(threshold, abs=1e-12)` (sanity: mean equals threshold exactly) and `not (avg_mean_boundary < threshold)` (sanity: avg-window would NOT TRIGGER because boundary equality fails strict <), and asserts `should_resurrect(H, …) == set()` (per-step NO TRIGGER because strict `<` rejects equality). Update the test docstring to mention Sub-assertion 5 in the closing summary and add a brief description of the strict less-than boundary. Verify by `grep -n "Sub-assertion 5" tests/test_safeguards.py` showing the description block, AND `grep -n "H_boundary" tests/test_safeguards.py` showing the variable definition, AND `uv run pytest tests/test_safeguards.py::test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history -v` PASSING (the same test invocation must pass both Sub-assertions 4 and 5 alongside the original 1-3).