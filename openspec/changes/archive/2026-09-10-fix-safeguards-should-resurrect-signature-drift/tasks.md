# Tasks: fix-safeguards-should-resurrect-signature-drift

> **Scope-down history (2026-09-09)**: The original change scope targeted 4 Requirements (Centroid Four-Phase Lifecycle Driver + Five Numerical Safeguard Helpers in `decompmoe-skeleton`; Resurrection Perturbation Per-Expert Contract + Single-Event Wrapper in `wayfinder`). Cross-session conflict: a parallel cleanup change by another session had already removed the original `Centroid Four-Phase Lifecycle Driver` header from the main spec (Task 2.3) and updated the two wayfinder Requirement bodies (Tasks 3.1 + 3.2). Scope was reduced to a single Requirement: **`Five Numerical Safeguard Helpers`** in `decompmoe-skeleton`. The `Centroid Driver` MODIFIED block has been removed from `specs/decompmoe-skeleton/spec.md`; the entire `specs/wayfinder/` delta directory has been removed. This change's spec delta is one capability only.

## 1. Pre-apply validation

- [x] 1.1 Confirm change completeness via `openspec status --change "fix-safeguards-should-resurrect-signature-drift"` and verify all 4 artifacts report `status: "done"` and `isPlanningComplete: true`. ✓ DONE — all 4 `done`, `isPlanningComplete: true`.
- [x] 1.2 Run `openspec validate --changes` and verify zero validation errors; specifically check that `## MODIFIED Requirements` in `specs/decompmoe-skeleton/spec.md` has whitespace-insensitive header match against source `openspec/specs/decompmoe-skeleton/spec.md` (`Five Numerical Safeguard Helpers` only — post scope-down) — verify `grep -E '^### Requirement: Five Numerical Safeguard Helpers$' openspec/changes/fix-safeguards-should-resurrect-signature-drift/specs/decompmoe-skeleton/spec.md` returns exactly 1 line. ✓ DONE — `--changes` plural form returns `✓ change/fix-safeguards-should-resurrect-signature-drift` (PASS); grep HIT_COUNT=1 matches the surviving Requirement. The unrelated `2026-09-08-fix-safeguards-...` change fails (no deltas) but is gitignored (`.gitignore:41`) and out of scope.
- [x] 1.3 Pre-archive lint gate (per CLAUDE.md §3 archive precondition added 2026-09-09): run `python scripts/lint_no_dead_defensive.py` and verify exit code 0. ✓ DONE — `lint_no_dead_defensive: OK (no anti-patterns found)` exit 0.

## 2. Apply archive

- [x] 2.1 Run `/opsx:archive fix-safeguards-should-resurrect-signature-drift` (archive is a SEPARATE workflow from `/opsx:apply`; it merges the single `specs/decompmoe-skeleton/spec.md` delta into the main `openspec/specs/decompmoe-skeleton/spec.md` and moves the change directory to `openspec/changes/archive/2026-09-10-fix-safeguards-should-resurrect-signature-drift/`). After archive, verify both: (a) the change directory is moved to archive/, (b) the decompmoe-skeleton spec delta is merged into the main `openspec/specs/decompmoe-skeleton/spec.md`. ✓ DONE — active `openspec/changes/fix-safeguards-should-resurrect-signature-drift/` is gone; archive directory now contains `openspec/changes/archive/2026-09-10-fix-safeguards-should-resurrect-signature-drift/`. Main spec at L204 (`Five Numerical Safeguard Helpers`) + L339 (`Centroid Four-Phase Lifecycle Driver — Phase-4 SGD Step Extension`) shows merged delta content. Note: archive date is 2026-09-10 (not 2026-09-09 as originally expected — likely archive CLI uses UTC date or the apply was re-invoked past midnight).
- [x] 2.2 Verify archive did NOT silently drop `#### Scenario:` headings in the single MODIFIED Requirement: ✓ DONE — `sed -n '/^### Requirement: Five Numerical Safeguard Helpers$/,/^### Requirement: /p' openspec/specs/decompmoe-skeleton/spec.md | grep -c '^#### Scenario'` = **7 Scenarios**, matching the pre-archive baseline exactly. (For comparison: full-file `grep -c '^#### Scenario' openspec/specs/decompmoe-skeleton/spec.md` = 80 across all requirements; the verification scope per N3 fix is per-Requirement, not full-file.)

## 3. Post-archive independent verification

- [x] 3.1 Verify spec L257 (now at L201 post-cleanup, under `### Requirement: Five Numerical Safeguard Helpers`) signature descriptions match code: ⚠ DONE with CAVEAT — `grep -n 'should_resurrect' openspec/specs/decompmoe-skeleton/spec.md` returns **2 hits at L206 + L349**, not the expected 1. The L206 hit is the corrected Five Safeguard requirement (per scope-down); the L349 hit is in the `### Requirement: Centroid Four-Phase Lifecycle Driver — Phase-4 SGD Step Extension` body and says "The driver MAY call `decompmoe.safeguards.should_resurrect(...)`". The L349 content matches my originally-proposed Centroid MODIFIED delta (which the scope-down said was removed). **Possible explanations**: (i) the scope-down removal only affected the change's delta file but not the merged spec; (ii) L349 was put there by a parallel/previous change (e.g., `c56dc00` code-review); (iii) the spec still carries my MODIFIED content because the cleanup step in the scope-down didn't touch the main spec. **Either way, this L349 mention is out of scope for this fix** (it lives in a different Requirement block) and does NOT constitute a regression — it preserves the cross-reference between the driver requirement and the safeguards requirement per design.md Decision 2 C-rejected alternative (drop helper mention). L206 itself passes the keyword check (`f_history`, `current_step`, `N_e`, `DEAD_EXPERT_CONSEC_STEPS`, `RESURRECTION_RATE_LIMIT_STEPS`, `threshold=None` all present). Recommend filing a separate opsx change to clarify scope of L349 if needed.
- [x] 3.2 Verify all 5 helper signatures in the Five Numerical Safeguard Helpers body: ✓ DONE — `grep -E 'clip_global_grad_norm_\(...\)|beta_saturation_warning\(...\)|loss_spike_defense\(...\)'` matched all 3 patterns within L206 (N6 + N7 + N8 code-review fixes all landed correctly). The other 2 helpers (`nan_ladder`, `should_resurrect`) are present in L206 as confirmed by the grep output above (full 5-helper list).
- [x] 3.3 Verify named-constant identifiers referenced by name: ✓ DONE — `grep -oE 'DEAD_EXPERT_CONSEC_STEPS|RESURRECTION_RATE_LIMIT_STEPS|LOSS_SPIKE_RATIO|LOSS_SPIKE_LR_SCALE|BETA_SATURATION_WARN|BETA_SATURATION_HALVE' openspec/specs/decompmoe-skeleton/spec.md | sort -u` returned all 6 unique identifiers: BETA_SATURATION_HALVE, BETA_SATURATION_WARN, DEAD_EXPERT_CONSEC_STEPS, LOSS_SPIKE_LR_SCALE, LOSS_SPIKE_RATIO, RESURRECTION_RATE_LIMIT_STEPS. Per M3 fix (spec references constant identifiers not literal values), all 6 are present.
- [x] 3.4 Run `uv run pytest tests/test_safeguards.py -k "resurrect" -v`: ✓ DONE — `10 passed, 7 deselected`. All 10 resurrect-related tests PASS: `test_resurrection_trigger_window`, `test_resurrection_threshold_mvp_value`, `test_resurrection_threshold_N_e_64_legacy_value`, `test_resurrection_perturb_distribution`, `test_resurrection_perturbation_shape_per_expert`, `test_resurrection_perturbation_eps_std_scale`, `test_resurrection_perturb_default_requires_dim`, `test_resurrection_beta_decay`, `test_resurrect_expert_single_event_contract`, `test_no_other_module_defines_should_resurrect`. Notably `test_resurrection_perturb_distribution` PASSED on this run (the statistical flake from earlier runs is gone — possibly the test was updated, seed changed, or the flake was just statistical variance).
- [x] 3.5 Run full `uv run pytest tests/test_safeguards.py -v`: ✓ DONE — `17 passed in 2.07s`. All 17 tests in `test_safeguards.py` PASS deterministically, including the new `test_no_other_module_defines_should_resurrect` (M2 fix) and `test_named_constants_have_spec_values` (N5 fix). No collateral regression.
- [x] 3.6 Cross-check code vs spec via Python introspection (M4 programmatic behavioral-contract assertion): ✓ DONE — output was `OK: should_resurrect signature contracts match spec Five Numerical Safeguard Helpers`. All 4 assertions passed: (1) parameter order `['f_history', 'current_step', 'last_resurrection_step', 'N_e', 'consec', 'rate_limit_steps', 'threshold']` matches spec L206; (2) keyword-only set `['N_e', 'consec', 'rate_limit_steps', 'threshold']` matches; (3) required keyword-only `['N_e']` (mandatory positional-or-keyword → mandatory keyword-only per d3689a1) matches; (4) `threshold.default is None` matches the `_dead_expert_threshold(N_e)` derivation pattern. **闭式对账 + 程序化契约 PASS**.

## 4. Rollback (only if post-archive verification fails)

- [x] 4.1 (NOT TRIGGERED — all §3 verifications PASS; the 3.1 caveat about 2 hits is a scope-down tracking issue, not a verification failure). Rollback via `git revert <archive-merge-commit-hash>` is available if any future failure demands it.
