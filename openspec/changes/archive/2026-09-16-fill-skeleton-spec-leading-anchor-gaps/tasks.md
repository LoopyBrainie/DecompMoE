## 1. Renumber the 5 existing skeleton-spec anchors to document order

- [x] 1.1 Edit `openspec/specs/decompmoe-skeleton/spec.md` and rename the 5 existing leading anchors to match document-order numbering: `req-1` (L349) → `req-18`, `req-2` (L385) → `req-19`, `req-3` (L405) → `req-20`, `req-4` (L430) → `req-21`, `req-5` (L455) → `req-22`. Verify by `grep -nE '<a id="req-(1[0-9]|2[0-2])">' openspec/specs/decompmoe-skeleton/spec.md` returning exactly 5 matches at lines 349, 385, 405, 430, 455.

## 2. Insert 17 new leading anchors (req-1 through req-17) before the anchorless Requirements at L8–L331

- [x] 2.1 In `openspec/specs/decompmoe-skeleton/spec.md`, insert `<a id="req-1"></a>` followed by one blank line immediately before `### Requirement: Canonical Package And Version Identifier` at L8; verify by `grep -nB1 '^### Requirement: Canonical Package' openspec/specs/decompmoe-skeleton/spec.md` showing `<a id="req-1"></a>` two lines above the heading.
- [x] 2.2 Insert `<a id="req-2"></a>` before `### Requirement: Total And Active Parameter Estimator` at L20; insert `<a id="req-3"></a>` before `### Requirement: Active FLOPs Parity Against Dense Baseline` at L36; insert `<a id="req-4"></a>` before `### Requirement: Wire-Level Contracts` at L56; insert `<a id="req-5"></a>` before `### Requirement: Inverse-Temperature Sigmoid With Gradient Bounds` at L64; verify by `grep -c '<a id="req-' openspec/specs/decompmoe-skeleton/spec.md` returning ≥ 9 (5 renumbered + 4 newly inserted in this task).
- [x] 2.3 Insert `<a id="req-6"></a>` before `### Requirement: Voronoi Self-Consistency Threshold` at L84; insert `<a id="req-7"></a>` before `### Requirement: C Extraction Four-Step Pipeline` at L100; insert `<a id="req-8"></a>` before `### Requirement: Isotropic Squared-Chord Distance And Logit` at L120; insert `<a id="req-9"></a>` before `### Requirement: Top-K Sparse Mask With Local Softmax` at L136; insert `<a id="req-10"></a>` before `### Requirement: Standard SwiGLU Expert With No Shared Branch` at L152; verify by `grep -c '<a id="req-' openspec/specs/decompmoe-skeleton/spec.md` returning ≥ 14.
- [x] 2.4 Insert `<a id="req-11"></a>` before `### Requirement: Loss Composition With Staged Lambda` at L176; insert `<a id="req-12"></a>` before `### Requirement: Five Numerical Safeguard Helpers` at L204; insert `<a id="req-13"></a>` before `### Requirement: Five-Phase Schedule State Machine` at L264; insert `<a id="req-14"></a>` before `### Requirement: Six Visualization Module Protocol Stubs` at L284; verify by `grep -c '<a id="req-' openspec/specs/decompmoe-skeleton/spec.md` returning ≥ 18.
- [x] 2.5 Insert `<a id="req-15"></a>` before `### Requirement: Hard-Constraint Grep Invariants` at L300; insert `<a id="req-16"></a>` before `### Requirement: Centroid Driver Semantic Invariants` at L315; insert `<a id="req-17"></a>` before `### Requirement: Centroid Driver Invariant Test Scenarios` at L331; verify by `grep -c '<a id="req-' openspec/specs/decompmoe-skeleton/spec.md` returning exactly 22.

## 3. Final verification and lint gates

- [x] 3.1 Run `grep -c '^### Requirement:' openspec/specs/decompmoe-skeleton/spec.md` and confirm the count equals 22 (Requirement heading count is unchanged from pre-change state); run `grep -c '<a id="req-' openspec/specs/decompmoe-skeleton/spec.md` and confirm the count equals 22 (anchor count matches Requirement count).
- [x] 3.2 Run `grep -oE '<a id="req-[0-9]+">' openspec/specs/decompmoe-skeleton/spec.md | sort -u` and confirm the unique IDs equal exactly `{req-1, req-2, ..., req-22}` (22 unique IDs, no duplicates); the set can be cross-checked with `python -c "import re; ids=sorted({int(re.search(r'req-(\d+)', x).group(1)) for x in __import__('subprocess').check_output(['grep','-oE','<a id=\"req-[0-9]+\">','openspec/specs/decompmoe-skeleton/spec.md'], text=True).splitlines()}); print(ids); assert ids==list(range(1,23)), ids"` returning `[1, 2, ..., 22]`.
- [x] 3.3 Run `python scripts/lint_no_dead_defensive.py` and confirm `exit=0`; run `python scripts/lint_no_source_field_drift.py` and confirm `exit=0` (no `Source:` field edits, no new defensive patterns introduced).
- [x] 3.4 Verify Requirement text and `**Source:**` annotations are unchanged by running `git diff --stat openspec/specs/decompmoe-skeleton/spec.md` showing only inserted lines (no deletions of existing content lines); cross-check by `git diff openspec/specs/decompmoe-skeleton/spec.md | grep '^-'` matching only the 5 lines being renumbered (`-<a id="req-1">`, `-<a id="req-2">`, `-<a id="req-3">`, `-<a id="req-4">`, `-<a id="req-5">`) and no other deletions.

## 4. Fix pre-existing text drift flagged by code-review

- [x] 4.1 Replace the brittle "21 Requirements × 34 Scenarios" count in `openspec/specs/decompmoe-skeleton/spec.md:4` `## Purpose` paragraph with the durable description "geometric-routing design of the main `wayfinder` spec" — the actual counts have drifted (skeleton now 22 R / 80 S; wayfinder 33 R / 78 S); removing the count prevents future drift. Verify by `grep -nE 'Requirements × [0-9]+ Scenarios' openspec/specs/decompmoe-skeleton/spec.md` returning zero matches.
- [x] 4.2 Mirror the same fix in `src/decompmoe/__init__.py` docstring (lines 3–5) — the docstring duplicates the same outdated count; replace "21 Requirements × 34 Scenarios of `openspec/specs/wayfinder/spec.md`" with "geometric-routing design of `openspec/specs/wayfinder/spec.md`". Verify by `grep -nE 'Requirements × [0-9]+ Scenarios' src/decompmoe/__init__.py` returning zero matches.
- [x] 4.3 Re-run `python scripts/lint_no_dead_defensive.py` and `python scripts/lint_no_source_field_drift.py` to confirm `exit=0` after the text edits (Purpose paragraph is metadata, not a `**Source:**` field; lint should be unaffected).
