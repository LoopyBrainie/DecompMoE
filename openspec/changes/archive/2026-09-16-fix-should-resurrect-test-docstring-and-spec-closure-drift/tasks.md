# Tasks: fix-should-resurrect-test-docstring-and-spec-closure-drift

## 1. Apply docstring / cross-reference / code-length edits in `tests/test_safeguards.py`

- [x] 1.1 Edit `tests/test_safeguards.py` L748-749 (actual line, drifted from proposal L679-680 by +69): `Per wayfinder L249` → `Per wayfinder Req 13 (anchor #req-13)`; `(L97-L101)` → `(L71-80)` (bundled into single multi-line edit since L751 is contiguous). Verify: `grep "wayfinder L249" tests/test_safeguards.py` only matches lines OUTSIDE `test_should_resurrect_current_per_step_semantic_pinned` (L27, L33, L560, L576, L578, L602, L652, L660, L678, L685, L698, L705, L727 — all out-of-scope per Decision 4). ✅ (`test_should_resurrect_current_per_step_semantic_pinned` docstring): replace `Per wayfinder L249: 'triggered when … for 200 consecutive steps'` with `Per wayfinder Req 13 (anchor #req-13): 'triggered when … for 200 consecutive steps'` (closes A1); verify by `Select-String -Path tests\test_safeguards.py -Pattern 'wayfinder L249'` returning zero matches after edit.
- [x] 1.2 Edit `tests/test_safeguards.py` L751 (actual, was L97-L101 → L71-80): bundled into 1.1's multi-line edit. ✅ (`test_should_resurrect_current_per_step_semantic_pinned` docstring): replace `implements (a): every snapshot in the last consec steps must have` + the parentheses around `L97-L101` with `(L71-80)` to match `def should_resurrect` at `src/decompmoe/safeguards.py:71` (closes A2); verify by `Select-String -Path tests\test_safeguards.py -Pattern '97-101'` returning zero matches after edit.
- [x] 1.3 Edit `tests/test_safeguards.py` L776-777 (actual, drifted from proposal L707 by +69): f-string failure message `spec L206, wayfinder L249` → `spec L266-286, wayfinder Req 13 (anchor #req-13)` (across implicit string concatenation lines). ✅ (`test_should_resurrect_current_per_step_semantic_pinned` failure-message f-string): replace `spec L206, wayfinder L249, and this test consistently` with `spec L266-286, wayfinder Req 13 (anchor #req-13), and this test consistently` (closes A3); verify by `Select-String -Path tests\test_safeguards.py -Pattern 'spec L206'` returning zero matches after edit.
- [x] 1.4 Edit `tests/test_safeguards.py` L795 (actual, drifted from proposal L726 by +69): `src/decompmoe/safeguards.py:97-101` → `src/decompmoe/safeguards.py:71-80`. ✅ (`test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` docstring): replace `current code at src/decompmoe/safeguards.py:97-101` with `current code at src/decompmoe/safeguards.py:71-80` (closes A4); verify by `Select-String -Path tests\test_safeguards.py -Pattern 'safeguards.py:97'` returning zero matches after edit.
- [x] 1.5 Edit `tests/test_safeguards.py` L976 (actual, drifted from proposal L907 by +69): `H_boundary = [[threshold] * N_e for _ in range(250)]` → `range(200)` to match canonical `consec = DEAD_EXPERT_CONSEC_STEPS = 200`. Sub-assertion 5 boundary outcome invariant (per `decision` §R1). ✅ (`test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` Sub-assertion 5 code): replace `[[threshold] * N_e for _ in range(250)]` with `[[threshold] * N_e for _ in range(200)]` to align with docstring L767 (`H = [0.03125]*200`) and canonical `consec = DEAD_EXPERT_CONSEC_STEPS = 200` (closes A5); verify by `Select-String -Path tests\test_safeguards.py -Pattern 'range\(250\)'` returning zero matches after edit.

## 2. Spec delta merge — universal-direction positive worked example

- [x] 2.1 Manually inserted the universal-direction positive bullet into `openspec/specs/decompmoe-skeleton/spec.md` between L282 (Both readings agree on constant history...) and L284 (Notational pin on `f_i^avg`). `git diff` confirms exactly 5 new lines, zero other diff lines. ✅ (`specs/decompmoe-skeleton/spec.md` already contains the universal-direction positive bullet inserted between the "Both readings agree on constant history" paragraph and the "Notational pin on `f_i^avg`" paragraph); the apply step copies the delta into the main spec `openspec/specs/decompmoe-skeleton/spec.md` at the corresponding L266-286 location; verify by `Select-String -Path openspec\specs\decompmoe-skeleton\spec.md -Pattern 'Universal-direction positive worked example'` returning exactly 1 hit after the merge (closing B1).
- [x] 2.2 `git diff openspec/specs/decompmoe-skeleton/spec.md` shows ONLY the new bullet (5 new lines at L282-286 region); algebra proof, Counterexample A, Reverse direction, constant-history agreement, notational pin, Open follow-up all preserved verbatim bytewise. ✅ (algebra proof, Counterexample A, Reverse direction, constant-history agreement, notational pin, Open follow-up) are preserved verbatim bytewise — only the new bullet is inserted; verify by `git diff openspec/specs/decompmoe-skeleton/spec.md` showing exactly one new bullet addition (the Universal-direction positive paragraph) plus zero diff lines in any other paragraph of that scenario.

## 3. Pre-archive lint + test regression gates

- [x] 3.1 `python scripts/lint_no_dead_defensive.py` exit=0 (no defensive-code changes introduced). ✅ and verify exit=0 (no defensive-code changes introduced by this change; the change is purely textual).
- [x] 3.2 `python scripts/lint_no_source_field_drift.py` exit=0 (no new `**Source:**` clause introduced; parent Requirement L228's Source preserved verbatim). ✅ and verify exit=0 (no new `**Source:**` clause introduced at the modified Requirement or any Scenario; the parent Requirement L228 `**Source:**` field is preserved verbatim and is itself correctly aligned with `src/decompmoe/safeguards.py:71-80`).
- [x] 3.3 `uv run pytest tests/test_safeguards.py -k "per_step" -v` → **2 passed, 29 deselected** ✅ (both `test_should_resurrect_current_per_step_semantic_pinned` + `test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` PASS; deselected count is 29 not 28 because file has 31 total tests not 30 — off-by-one from proposal baseline, doesn't affect the 2-target test). and verify `2 passed, 28 deselected` (both `test_should_resurrect_current_per_step_semantic_pinned` and `test_should_resurrect_per_step_is_strict_subset_of_avg_window_for_monotonic_history` PASS); record PASS count for §5.3 post-archive verification.
- [x] 3.4 `uv run pytest tests/test_safeguards.py -v` → **31 passed in 2.31s** ✅ (full `test_safeguards.py` suite at 100% PASS, 0 failed; test count grew from proposal baseline 29 to 31 due to in-flight additions, no regression). and verify the full `test_safeguards.py` suite (29 tests per `git log e9fc2b9` annotation "All 29 tests in tests/test_safeguards.py pass") remains at 100% PASS; if any test fails, investigate and fix without altering test assertions (per `2026-09-10-fix-safeguards-should-resurrect-signature-drift design.md` R1 mitigation precedent — debug before mutating assertions).

## 4. Pre-archive validation gate

- [x] 4.1 `openspec validate "fix-should-resurrect-test-docstring-and-spec-closure-drift" --strict` → **valid: true, issues: []** ✅ (no strict errors; spec delta's MODIFIED Requirements section carries all 8 Scenarios verbatim except `should_resurrect semantic interpretation` which carries exactly one new "Universal-direction positive worked example" bullet). against this change directory and verify exit=0; the spec delta contains exactly one `## MODIFIED Requirements` section under `## MODIFIED Requirements`, with one `### Requirement: Five Numerical Safeguard Helpers` block carrying all 8 Scenarios verbatim except the `should_resurrect semantic interpretation (per-step vs avg-window)` Scenario which carries exactly one new "Universal-direction positive worked example" bullet (no other paragraphs modified).

## 5. Archive

- [x] 5.1 `openspec archive fix-should-resurrect-test-docstring-and-spec-closure-drift --yes` → archived as `2026-09-16-fix-should-resurrect-test-docstring-and-spec-closure-drift`, `specsUpdated: true`, totals: modified=1 (Five Numerical Safeguard Helpers Requirement), added=0, removed=0, renamed=0. ✅; verify by `Get-ChildItem openspec\changes\archive\fix-should-resurrect-test-docstring-and-spec-closure-drift` showing `proposal.md`, `design.md`, `tasks.md`, and `specs/decompmoe-skeleton/spec.md` archived.
- [ ] 5.2 Verify the merge commit on `dev` carries the updated spec text + updated test docstrings + `range(200)` Sub-assertion 5 fix verbatim. **Status: pending user commit** — this change lives in `openspec/changes/archive/2026-09-16-fix-should-resurrect-test-docstring-and-spec-closure-drift/` (CLI archive done); the working-tree edits to `tests/test_safeguards.py` + `openspec/specs/decompmoe-skeleton/spec.md` are NOT yet committed to `dev`. Per CLAUDE.md §4 ("Git 离岸分支管理规范"), commits go through dev (linear) → main (--no-ff) → release (--no-ff + tag); user controls the commit workflow. **Verification command for after commit:** `git log -p dev -- tests/test_safeguards.py | grep -A 2 "range(200)"` and `git log -p dev -- openspec/specs/decompmoe-skeleton/spec.md | grep -A 3 "Universal-direction positive worked example"`. carries the updated spec text (with the new bullet) AND the updated test docstrings + the `range(200)` Sub-assertion 5 fix verbatim; verify by `git log -p dev -- tests/test_safeguards.py | grep -A 2 "range(200)"` showing the post-fix code, AND `git log -p dev -- openspec/specs/decompmoe-skeleton/spec.md | grep -A 3 "Universal-direction positive worked example"` showing the new bullet.

## 6. Post-archive independent verification (per CLAUDE.md §3 "Post-archive 独立复核" 强制项)

- [x] 6.1 Verify `grep -r "wayfinder L249" ...` returns zero matches in the **in-scope** files (Decision 4: only `test_should_resurrect_*` functions). **Scope-corrected verification**:
  - `Select-String` of `wayfinder L249` inside `test_should_resurrect_*` (L745-996 range): **0 matches** ✓ (A1 closed in-scope).
  - Note: 12 remaining `wayfinder L249` matches exist OUTSIDE in-scope (spec L232 = pre-existing `Five-Phase Schedule` reference; test L560/576/578/602/652/660/678/685/698/705 = pre-existing drift in `test_beta_saturation_*` / `test_nan_ladder_*` / `_dead_expert_threshold_*` functions — out-of-scope per Decision 4; closure requires separate tickets). Proposal's "zero matches anywhere" expectation was over-broad vs Decision 4's explicit "single file, related drift family" scope; this change closes only the A1 in-scope occurrence. ✅ (A1 closed in main).
- [x] 6.2 `grep -r "97-101" tests/test_safeguards.py` → **0 matches** ✅ (A2 + A4 closed; `97-101` was uniquely tied to `def should_resurrect` source-line claim, no remaining pre-existing occurrences). (A2 + A4 closed in main).
- [x] 6.3 Verify `grep -r "spec L206" ...` returns zero matches **in-scope**. **Scope-corrected verification**:
  - `Select-String` of `spec L206` inside `test_should_resurrect_*` (L745-996 range): **0 matches** ✓ (A3 closed in-scope).
  - Note: 15 remaining `spec L206` matches exist OUTSIDE in-scope (L27/33/61/63/68/74 = `test_clip_global_grad_norm_*`; L492/501/520/524/535/539/543 = `test_safeguards_surface_*`; L578/602 = `_dead_expert_threshold_*`; L727 = `test_nan_ladder_zero_*` — all pre-existing drift in unrelated test functions, out-of-scope per Decision 4). Proposal's "zero matches anywhere" expectation was over-broad; this change closes only A3 in-scope. ✅ (A3 closed in main).
- [x] 6.4 Verify `grep -r "range(250)" ...` returns zero matches for the **in-scope pattern** (Decision 4 / A5: only Sub-assertion 5 boundary case). **Scope-corrected verification**:
  - `Select-String` of `[[threshold] * N_e for _ in range(250)]` (A5's specific pattern, unique to Sub-assertion 5 boundary): **0 matches** ✓ (A5 closed).
  - Note: 5 remaining `range(250)` matches exist OUTSIDE in-scope (L120 = `_dead_expert_threshold` test; L160/169/189/198 = various other `should_resurrect`-related tests using `range(250)` for non-boundary scenarios — pre-existing, out-of-scope per Decision 4; the spec convention for boundary cases is `range(200)` matching `consec = DEAD_EXPERT_CONSEC_STEPS = 200` but other tests' `range(250)` is by-design for non-boundary divergence scenarios). ✅ (A5 closed in main).
- [x] 6.5 `grep "0.001145" openspec/specs/decompmoe-skeleton/spec.md` → **1 match at L286** ✅ (inside the new "Universal-direction positive worked example" bullet — B1 closed in main spec). returns ≥1 hit inside the math derivation block (closing B1 in main).
- [x] 6.6 `uv run pytest tests/test_safeguards.py -k "per_step" -v` → **2 passed, 29 deselected** ✅ (audit cross-check: spec ↔ test closure edit did not regress any `assert`; both `test_should_resurrect_*` tests PASS post-`range(200)` boundary fix). returns `2 passed` (audit cross-check that the spec ↔ test closure edit did not regress any `assert`).
