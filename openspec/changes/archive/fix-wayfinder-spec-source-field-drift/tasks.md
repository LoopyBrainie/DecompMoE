## 1. CLAUDE.md governance wording micro-adjust

- [x] 1.1 In `CLAUDE.md` §2 L19 (rule 3) replace `反链到 \`wayfinder/tickets/*.md\`` with `必须含 \`wayfinder/tickets/<ID>.md\` 字面反链(pure ticket 或 \`(historical, <原值>; superseded by <change> Decision N)\` 标注格式均可),可附加 \`change <name> design.md (Decision N)\` 反链作为补充引用` and verify the line still parses as one bullet under §2 ordered list
- [x] 1.2 In `CLAUDE.md` §3 L31 (the bullet starting `每次 Spec 变更必须含`) replace with `每次 Spec 变更必须含 \`**Source:**\` 反链 ticket(\`wayfinder/tickets/<ID>.md\` 字面必备),允许附加 change Decision 反链;ticket 当前值与 spec 当前值不一致时使用 \`(historical, <原值>; superseded by <change> Decision N)\` 标注格式;lint 硬卡见 \`scripts/lint_no_source_field_drift.py\`` and verify the bullet remains under the §3 list
- [x] 1.3 In `CLAUDE.md` §3 (the lint-gate bullet starting with `\`/opsx:archive\` 前置条件`) leave the bullet as single-script (only `python scripts/lint_no_dead_defensive.py`) and append a sentence clarifying that `scripts/lint_no_source_field_drift.py` is **informational-only** pending the follow-up change `migrate-l678-source` (atomic gate wiring happens in the follow-up, not in this change). Verify the bullet remains under §3 and the wording explicitly distinguishes "gate" from "informational-only" scripts.

## 2. Patch Source fields in `openspec/specs/wayfinder/spec.md`

Each task patches one Source field. Verification for each: `grep -nE "^\*\*Source:\*\*" openspec/specs/wayfinder/spec.md | awk '{print $1}' | xargs -I{} sed -n '{},+0p' openspec/specs/wayfinder/spec.md | grep "wayfinder/tickets/"` returns the patched line, AND `python scripts/lint_no_source_field_drift.py openspec/specs/wayfinder/spec.md` exits 0 for that line.

- [x] 2.1 L525 (CentroidDriver Dual-Channel Architecture Contract): replace Source with `**Source:** \`wayfinder/tickets/A6b-1.md\` (historical, dual-channel split for phase freeze rule; Driver/Gradient channel structure added by \`fix-openspec-doc-bugs\` Decision 2), change \`fix-openspec-doc-bugs\` design.md (Decision 2)` and verify the line contains `wayfinder/tickets/A6b-1.md` literally
- [x] 2.2 L543 (Operational Domain γ' Reset Closed-Form Worked Example): replace Source with `**Source:** \`wayfinder/tickets/A4-1.md\` (historical, γ parameterization origin: \`β_min + (β_max−β_min)·σ(γ)\`), \`wayfinder/tickets/A6b-2.md\` (historical, phase-boundary optimizer state policy); change \`fix-math-consistency-audit-2026-08\` design.md (Decision 2 — closed-form \`γ' = ln((β_{p3}−1)/(32−β_{p3}))\` added by this change)` and verify both ticket literals present
- [x] 2.3 L558 (Phase 2 β Box Equality): replace Source with `**Source:** \`wayfinder/tickets/A6b-1.md\` (historical, A6b-1 phase-ramp design intent; β box endpoints (1.0, 4.0) and (4.0, 16.0) added by \`fix-math-consistency-audit-2026-08\` Decision 3), change \`fix-math-consistency-audit-2026-08\` design.md (Decision 3)` and verify the ticket literal present
- [x] 2.4 L594 (β^eff Phase 3 → 4 Continuity Closed-Form): replace Source with `**Source:** \`wayfinder/tickets/A6b-2.md\` (historical, phase-boundary continuity invariant; closed-form \`γ'\` derivation added by \`fix-math-consistency-audit-2026-08\` Decision 2), change \`fix-math-consistency-audit-2026-08\` design.md (Decision 2)` and verify the ticket literal present
- [x] 2.5 L609 (Closed-Form Gradient Bound Worst Case): replace Source with `**Source:** \`wayfinder/tickets/A4-1.md\` (historical, MAX_GRAD_PER_C = β_max = 32 bound declared in A4-1; worst-case attainability verification at orthogonal unit vectors added by \`fix-math-consistency-audit-2026-08\` Decision 6), change \`fix-math-consistency-audit-2026-08\` design.md (Decision 6)` and verify the ticket literal present
- [x] 2.6 L628 (Forward Formula Numerical Verification — Routing Layer): replace Source with `**Source:** \`wayfinder/tickets/A2-2.md\` (historical, head-aggregated forward structure), \`wayfinder/tickets/A4-2.md\` (historical, top-k sparse mask + local softmax composition); change \`fix-math-consistency-audit-2026-08\` design.md (Decision 7 — closed-form numerical verification form \`x_out = x + Σp_i·E_i\` within \`abs=1e-6\` added by this change)` and verify both ticket literals present
- [x] 2.7 **DO NOT PATCH** L678 (Test Guard Precision for Closed-Form Numerical Claims): leave the Source field as-is. Verify after `python scripts/lint_no_source_field_drift.py openspec/specs/wayfinder/spec.md` runs against the patched file, L678 is reported as the single remaining violation with message `L678: source field missing wayfinder/tickets/ reference` and exit code is non-zero. This failure is the **intended design signal** captured in `design.md` Decision 4 and `proposal.md` §L678留待lint失败后由后续change闭环. Document L678 as open migration debt in the apply-phase summary.

## 3. Implement lint script

- [x] 3.1 Create `scripts/lint_no_source_field_drift.py` with the following structure (verify each component by reading back the file after writing): a `from __future__ import annotations` import, a module docstring stating the rule, a `SPECS_GLOB = "openspec/specs/**/spec.md"` constant, a `SOURCE_LINE_RE = re.compile(r"^\*\*Source:\*\*")` constant, a `REQUIRED_SUBSTRING = "wayfinder/tickets/"` constant, a `def iter_source_lines(paths: Iterable[Path]) -> Iterator[tuple[Path, int, str]]` generator that yields `(path, line_number, line_content)` for every line starting with `**Source:**` in any matched spec file, a `def lint_file(path: Path) -> list[tuple[int, str, str]]` function returning a list of `(line_no, line_content, reason)` violations where `reason = "source field missing wayfinder/tickets/ reference"`, a `def main() -> int` function that walks all matched spec files, prints per-violation `"{path}:{line_no}: {reason}: {content}"` lines to stdout (aligned with `scripts/lint_no_dead_defensive.py` output convention; L94/97/101-109 all use `print(...)` defaulting to stdout), returns `1` on any violation / `0` if clean. NO exemption table, NO `JUSTIFIED_EXEMPTIONS` registry, NO per-line opt-out.
- [x] 3.2 Add a `def main()` `__name__ == "__main__"` entrypoint that calls `main()` and `sys.exit`s with the returned code; verify by running `python scripts/lint_no_source_field_drift.py openspec/specs/wayfinder/spec.md` against the pre-patch file and observing exit code 1 with 7 violations (L525, L543, L558, L594, L609, L628, L678)
- [x] 3.3 Re-run `python scripts/lint_no_source_field_drift.py openspec/specs/wayfinder/spec.md` against the post-patch file (after tasks §2.1–§2.6) and verify exit code is **1** with exactly **1 violation (L678)** — the 6 patched lines must pass the substring check; L678 must remain the sole FAIL. This is the **expected signature** post-apply per the script docstring `KNOWN_OPEN_VIOLATIONS` section; this proves the script distinguishes "no ticket" from "ticket present" correctly. Note: per `proposal.md` Open Follow-ups, the follow-up change `migrate-l678-source` is responsible for transitioning exit code to 0 in the same change that re-wires the gate (CLAUDE.md §3 lint gate bullet).
- [x] 3.4 Verify the existing `scripts/lint_no_dead_defensive.py` is **not modified** by running `git diff scripts/lint_no_dead_defensive.py` and observing empty output; confirm `JUSTIFIED_EXEMPTIONS` registry line count is unchanged

## 4. Wire lint into archive precondition

- [x] 4.1 Verify `CLAUDE.md` §3 archive-precondition bullet (rolled back in §1.3 cleanup) lists **only** `scripts/lint_no_dead_defensive.py` and explicitly marks `scripts/lint_no_source_field_drift.py` as informational-only pending the follow-up change `migrate-l678-source` (atomic gate-wiring happens there, not here). Verify a hypothetical archive run invoking `python scripts/lint_no_dead_defensive.py` alone exits 0 (current state). The new lint script is **not** part of the gate this change.
- [x] 4.2 Run `python scripts/lint_no_dead_defensive.py` end-to-end (this is the current archive gate per CLAUDE.md §3) and capture exit code 0 + stdout output. Separately, run `python scripts/lint_no_source_field_drift.py` and capture exit code 1 + stdout output showing the **sole** violation is on L678; this is the informational-only signature per docstring `KNOWN_OPEN_VIOLATIONS`. Record both in the apply-phase verification log.

## 5. Full-spec audit (closes the change)

- [x] 5.1 Run `grep -nE "^\*\*Source:\*\*" openspec/specs/wayfinder/spec.md | awk -F: '{print $1}' | xargs -I{} sed -n '{},+0p' openspec/specs/wayfinder/spec.md | grep -v "wayfinder/tickets/"` and verify the output is exactly **1 line** (L678's pure-CLAUDE.md Source) — proves 6 of 7 violations closed, L678 intentional debt remains visible
- [x] 5.2 Run `openspec validate fix-wayfinder-spec-source-field-drift --type change` and verify exit code 0 (spec delta parses correctly, no missing-scenario errors). Soft-check: also verify the expected signature `grep -nE "^\*\*Source:\*\*" openspec/specs/wayfinder/spec.md | wc -l` returns 33 total, of which `grep "wayfinder/tickets/"` (via xargs + sed + grep) returns 32 PASS and 1 FAIL — any deviation (31/2, 33/0, etc.) indicates a regression in either a patch or in an external spec edit and must be investigated before archive.
- [x] 5.3 Run `python scripts/lint_no_source_field_drift.py` and capture exit code 1 with **exactly 1 violation** on L678 in the apply-phase verification log. Verify this signature matches the script docstring `KNOWN_OPEN_VIOLATIONS` declaration (req-33 L678 — governance-lineage Req). This lint failure is **expected** post-apply and is resolved by the follow-up change `migrate-l678-source`; it is NOT an archive-blocking bug because this change does not wire the new lint into the archive gate (gate wiring is atomic with L678 resolution in the follow-up).
