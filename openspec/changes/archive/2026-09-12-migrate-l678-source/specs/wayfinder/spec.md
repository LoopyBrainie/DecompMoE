## REMOVED Requirements

### Requirement: Test Guard Precision for Closed-Form Numerical Claims

**Reason**: design lineage is `CLAUDE.md` §6 第 8 条 (amended by commits `bec147d` 2026-09-07 + `83a0503` 2026-09-07), NOT any `wayfinder/tickets/*.md` ticket. Per the "governance-origin requirements trigger lint failure" Scenario in `wayfinder/spec.md` req-34 (L726-732), Requirements whose honest `**Source:**` annotation cannot be written (because no `wayfinder/tickets/` entry is their legitimate design predecessor) MUST be migrated to a separate governance capability before archive. This Requirement's prior Source line was the single violation reported by `scripts/lint_no_source_field_drift.py` (exit=1, 1 violation on the old L678 line) per the `fix-wayfinder-spec-source-field-drift` change that introduced the lint script.

**Migration**: the Requirement body + 5 Scenarios (old req-33 L662-L704) are moved verbatim to `openspec/specs/governance/spec.md` as the new capability's `Requirement: Test Guard Precision for Closed-Form Numerical Claims` (governance `req-gov-1`, anchor `<a id="req-gov-1">`). The moved Requirement's `**Source:**` field is rewritten in governance-compliant form (references `CLAUDE.md` §6 第 8 条 + amendment commits + `fix-wayfinder-spec-source-field-drift` design.md Decision 4 + this change's design.md Decision 1; no `wayfinder/tickets/` reference required because the file lives outside `openspec/specs/wayfinder/`). The 5 test anchors (`tests/test_config.py::*`, `tests/test_extraction.py::test_complexity_budget`, `tests/test_experts.py::test_expert_pool_param_count`, `tests/test_sphere.py::*` bisection witnesses + conversion-identity witness) are unchanged at the implementation level — only the spec-side citation of those tests moves from wayfinder to governance. Future audit trails cite `openspec/specs/governance/spec.md` req-gov-1 instead of `openspec/specs/wayfinder/spec.md` req-33 (the latter no longer exists post-archive).
