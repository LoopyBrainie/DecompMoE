# Tasks: migrate-l678-source

## 1. Verify planning artifacts match the option (a) Decision Record

- [x] 1.1 Verify `proposal.md` (## What Changes) explicitly cites option (a) "split capability migration" from `fix-wayfinder-spec-source-field-drift/proposal.md` "Open Follow-ups → migrate-l678-source" 4-option Decision Matrix, and that the proposal's Impact section lists all 4 atomic actions (governance spec added, wayfinder req-33 removed, CLAUDE.md §3 modified, lint script docstring trimmed); verify by `grep -n "option (a)\|governance\|CLAUDE.md §3\|KNOWN_OPEN_VIOLATIONS" openspec/changes/migrate-l678-source/proposal.md` returning hits for all 4 actions.
- [x] 1.2 Verify `specs/governance/spec.md` carries the moved Requirement verbatim (5 numbered obligations + 5 Scenarios identical to wayfinder req-33 pre-migration form) with rewritten `**Source:**` field referencing CLAUDE.md lineage + amendment commits + change Decision 反链; verify by `diff <(sed -n '/^### Requirement: Test Guard Precision/,/^$/p' openspec/specs/wayfinder/spec.md | head -50) <(sed -n '/^### Requirement: Test Guard Precision/,/^$/p' openspec/changes/migrate-l678-source/specs/governance/spec.md | head -50)` showing requirement body identical (only Source field rewrites + file location differs).
- [x] 1.3 Verify `specs/wayfinder/spec.md` delta uses `## REMOVED Requirements` header with Reason citing `CLAUDE.md` §6 第 8 条 lineage + req-34 migration mandate, and Migration citing the new governance capability path; verify by `grep -n "## REMOVED Requirements\|Reason:\|Migration:" openspec/changes/migrate-l678-source/specs/wayfinder/spec.md` returning hits for all 3 fields.

## 2. Apply tooling changes (lint script content + CLAUDE.md + lint script docstring)

- [x] 2.1 Modify `scripts/lint_no_source_field_drift.py` content to implement a per-capability Source rule: replace the single `REQUIRED_SUBSTRING = "wayfinder/tickets/"` constant with a small hardcoded mapping `REQUIRED_SUBSTRING_BY_PATH = {Path("openspec/specs/governance/spec.md"): "CLAUDE.md"}` (default `"wayfinder/tickets/"` for all other paths). The `lint_file` function dispatches per path; same content-based substring check (no exemption-table rot, no CLI flag, no env var). The docstring's "Background" + "Anti-patterns flagged" sections are updated to reflect the per-capability rule; the "Design constraints" section is preserved verbatim. Verify by `python scripts/lint_no_source_field_drift.py` exit=0 against (a) the post-archive spec tree (governance + wayfinder + decompmoe-skeleton), (b) a synthetic test case where a `wayfinder` spec line is missing `wayfinder/tickets/` (script must report violation), (c) a synthetic test case where a `governance` spec line is missing `CLAUDE.md` (script must report violation).
- [x] 2.2 Modify `CLAUDE.md` §3 L28: replace the `informational-only` clause + `migrate-l678-source` follow-up reference with the gate-wired form parallel to the `lint_no_dead_defensive.py` mention immediately above on the same bullet; verify by `grep -n "lint_no_source_field_drift" CLAUDE.md` showing exit=0 against the post-change form (the script name still appears but the gate wording reflects gate, not informational-only).
- [x] 2.3 Delete the `KNOWN_OPEN_VIOLATIONS` section (header + 17 lines) from `scripts/lint_no_source_field_drift.py` docstring; verify by `grep -n "KNOWN_OPEN_VIOLATIONS" scripts/lint_no_source_field_drift.py` returning no hits (the section is fully removed, not just edited).

## 3. Pre-archive lint gate

- [x] 3.1 Run `python scripts/lint_no_dead_defensive.py` and verify exit=0; if exit≠0, fix the lint violation or escalate before proceeding.
- [x] 3.2 Run `python scripts/lint_no_source_field_drift.py` against the post-spec-delta state (after tasks §1's spec file changes are applied) and verify exit=0 — the spec delta alone (governance capability added + wayfinder req-33 removed) should close the L678 violation; verify by inspecting the script's output showing 0 violations.

## 4. Atomicity verification

- [x] 4.1 Verify all 5 actions are present in `openspec/changes/migrate-l678-source/` artifacts before `/opsx:archive`: (a) `specs/governance/spec.md` exists and contains the moved Requirement, (b) `specs/wayfinder/spec.md` contains the `## REMOVED Requirements` block for req-33, (c) `scripts/lint_no_source_field_drift.py` content implements per-capability rule (governance → CLAUDE.md, others → wayfinder/tickets/), (d) `CLAUDE.md` §3 L28 wording changed in working tree, (e) `scripts/lint_no_source_field_drift.py` docstring `KNOWN_OPEN_VIOLATIONS` section deleted in working tree; verify by `ls openspec/changes/migrate-l678-source/specs/governance/spec.md && grep -c "^### Requirement: Test Guard Precision" openspec/changes/migrate-l678-source/specs/governance/spec.md && grep -c "## REMOVED Requirements" openspec/changes/migrate-l678-source/specs/wayfinder/spec.md && grep -c "REQUIRED_SUBSTRING_BY_PATH" scripts/lint_no_source_field_drift.py && grep -c "KNOWN_OPEN_VIOLATIONS" scripts/lint_no_source_field_drift.py` returning 1/1/1/1/0.

## 5. Archive

- [x] 5.1 Run `/opsx:archive` (or `openspec archive migrate-l678-source` if CLI invocation is preferred); the archive step will run both lint scripts as precondition (CLAUDE.md §3 gate now wired); verify by `ls openspec/changes/archive/2026-09-XX-migrate-l678-source/` (where XX is the archive date).
- [ ] 5.2 Verify the merge commit on `dev` carries all 5 atomic changes in a single commit; verify by `git log -p dev -- openspec/specs/governance/spec.md openspec/specs/wayfinder/spec.md CLAUDE.md scripts/lint_no_source_field_drift.py | head -100` showing all 4 files modified in the same diff hunk set. **DEFERRED — pending user decision on commit scope**: working tree has ~9 unrelated modifications from prior sessions (`src/decompmoe/{beta,safeguards,schedule}.py`, `tests/test_{beta,schedule}.py`, `apply-checklist.md`, `openspec/changes/archive/2026-09-11-enhance-safeguards-closed-form-tests/tasks.md`, plus several untracked archive directories and a garbled `D\357\200\272myProjectDecompMoEtest_principle.py` filename). The 5 atomic changes for `migrate-l678-source` are staged-ready but not committed; user must decide whether to (a) commit only this change's 5 files selectively, (b) batch-commit with the prior-session debris (NOT recommended — scope creep), or (c) revert the prior-session debris first then commit cleanly. Task is incomplete until user picks one.

## 6. Post-archive independent verification (per CLAUDE.md §3 "Post-archive 独立复核" 强制项)

- [x] 6.1 Verify `openspec validate --specs` accepts the new `governance` capability and the modified `wayfinder` capability (requirement count drops 34 → 33); verify by `openspec list --specs --json` returning 3 capabilities (governance, decompmoe-skeleton, wayfinder) with requirement counts `1 / 22 / 33`.
- [x] 6.2 Verify `python scripts/lint_no_source_field_drift.py` exits 0 against the merged main spec tree; verify by re-running the script and checking the output line `0 violation(s) found` + exit code 0.
- [x] 6.3 Verify `python scripts/lint_no_dead_defensive.py` exits 0 against the post-archive codebase; verify by re-running and checking exit code 0.
- [x] 6.4 Verify `scripts/lint_no_source_field_drift.py` docstring no longer contains the `KNOWN_OPEN_VIOLATIONS` section; verify by `grep -c "KNOWN_OPEN_VIOLATIONS" scripts/lint_no_source_field_drift.py` returning 0.
- [x] 6.5 Verify `CLAUDE.md` §3 L28 wording reflects both lint scripts as gate preconditions (NOT informational-only for `lint_no_source_field_drift.py`); verify by `sed -n '/^.*\/opsx:archive.*前置条件/,/^$/p' CLAUDE.md` showing the new parallel wording.

## 7. Post-archive independent review fixes (2026-09-12, per CLAUDE.md §3 "Post-archive 独立复核")

Independent code-review of the archived change surfaced 4 issues (1 Important + 2 Important + 1 Minor) — all addressed in-place on the post-merge live spec + tooling (no formal follow-up OpenSpec change; tracked here per audit-trail convention):

- [x] 7.1 **Fix Finding 1 (Important)**: spec math inconsistency — N_e=64 test literal `1.020507` was round-half-up, not 6dp truncation. Updated `openspec/specs/governance/spec.md` Scenario 5 test literals to `1.020506 rad` (the actual 6dp truncation of canonical `1.0205068335735599`). Also updated `tests/test_sphere.py::test_voronoi_canonical_N_e_dependence` assertion + docstring to match. Diff with abs=1e-6 is `8.34e-7` (well within tolerance; test passes).
- [x] 7.2 **Fix Finding 2 (Important)**: phantom test reference — `test_voronoi_rad_precision_alignment` was cited twice in spec (line 22 Source anchor list + line 47 OUT OF SCOPE list) but never existed in git history. Removed both references; preserved `test_versine_voronoi_closed_form` (which DOES exist at `tests/test_sphere.py:184`).
- [x] 7.3 **Fix Finding 3 (Important)**: ambiguous truncation wording — "truncates at the 7th decimal to `1.173548`" rewrote to "truncates to 6 decimal places (the 7th decimal digit `2` is dropped), yielding `1.173548`" for unambiguous meaning.
- [x] 7.4 **Fix Finding 6 (Minor)**: lint script hardcoded relative path fragility — `REQUIRED_SUBSTRING_BY_PATH_RELATIVE` keys were `Path("openspec/specs/governance/spec.md")` (hardcoded relative). Refactored to derive from `_REPO_ROOT = Path(__file__).resolve().parent.parent` via a `_repo_root()` helper, plus added `path.resolve()` in `required_substring_for()` so relative CLI args (e.g. `openspec/specs/governance/spec.md`) and absolute paths from `glob` both compare on equal terms. Also dropped the redundant `if path == k` absolute-equality check (suffix match now handles both forms after resolution).
- [x] 7.5 **Post-fix verification**: lint_no_source_field_drift.py exit=0 (3 files scanned), lint_no_dead_defensive.py exit=0, full pytest 43/43 passed (test_config + test_experts + test_extraction + test_sphere), synthetic regression cases 1-4 all pass (governance spec with wayfinder/tickets Source → flagged; wayfinder spec with CLAUDE.md Source → flagged; governance spec with CLAUDE.md Source → passes; main-spec post-archive state → passes with 0 violations).
