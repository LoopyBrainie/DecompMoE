## 1. Wayfinder spec anchor relocation

- [x] 1.1 `openspec/specs/wayfinder/spec.md` L442: `<a id="req-34">` → `<a id="req-35">`. Requirement body, 4 Scenarios, Source field L443-463 preserved verbatim. Verified `Select-String -Pattern 'id="req-35"'` returns exactly 1 hit at L442. ✅: replace `<a id="req-34"></a>` (currently attached to "CG n=1 boundary behavior") with `<a id="req-35"></a>`. **Anchor relocation only**; the Requirement body, 4 Scenarios, Source field, and L443-463 textual content are preserved verbatim. Verify by `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-35"'` returning exactly 1 hit at L442.
- [x] 1.2 `openspec/specs/wayfinder/spec.md` L676: inserted `<a id="req-34">` on new line before "### Requirement: Source Field Format Invariant for OpenSpec Specs" (now anchor at L676; Requirement body and Scenarios L678+ preserved verbatim). Verified `Select-String -Pattern 'id="req-34"'` returns exactly 1 hit at L676. ✅ area (before "### Requirement: Source Field Format Invariant for OpenSpec Specs"): insert `<a id="req-34"></a>` on a new line just before the Requirement heading. **Anchor restoration only**; the Requirement body and Scenarios are preserved verbatim (this restores the historical anchor that `f033d2e` failed to add when syncing `tighten-source-field-format-lint-2026-09/specs/wayfinder/spec.md`). Verify by `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-34"'` returning exactly 1 hit at the new L676-adjacent position.
- [x] 1.3 `governance/spec.md` L3 cross-reference "per the migration rule in `wayfinder/spec.md` req-34 'governance-origin requirements trigger lint failure' Scenario" now points to correct content: `Select-String` confirms the Scenario at L712 is now UNDER the new `req-34` anchor (L676). Cross-reference chain restored. ✅ "per the migration rule in `wayfinder/spec.md` req-34 'governance-origin requirements trigger lint failure' Scenario" now points to correct content: navigate to `req-34` in wayfinder spec (post-edit should be Source Field Format Invariant), confirm a Scenario titled "governance-origin requirements trigger lint failure" exists under it. Verify by `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'governance-origin requirements trigger lint failure' -Context 2` showing the Scenario appears under the new `req-34` location.
- [x] 1.4 No duplicate anchor IDs: `Group-Object` over all `req-N` patterns shows each anchor (req-1 through req-35 with historical gaps at req-6/10/16) appears EXACTLY once. 32 distinct anchors total. ✅ exist (HTML constraint): `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-' | Group-Object { $_.Line -replace '.*id="req-(\d+)".*','$1' } | Where-Object { $_.Count -gt 1 }` returns empty (each `req-N` ID appears exactly once).

## 2. Test math coverage — L2-norm sanity check

- [x] 2.1 `tests/test_metrics.py::test_cg_n_eq_1_returns_magnitude` (L171-): appended 1 new assertion after the 5 existing `pytest.approx(abs=1e-12)` assertions: `assert actual_l2_path == torch.linalg.norm(g_pos_1d).item(), f"actual CG=..."` — pins implementation path as L2-norm reduce, not abs(sum)/abs(max) implementations. Bare `==` because FP-exact (sqrt(g²) exact for |g| ≤ 2^52 in IEEE-754 binary64). Verified test PASSES with 6 assertions. ✅ (L171-211): after the existing 5 `pytest.approx(abs=1e-12)` assertions (L182-206), append a new assertion: `assert metrics.CG(g_pos_1d).item() == torch.linalg.norm(g_pos_1d).item(), f"actual CG={metrics.CG(g_pos_1d).item()}; L2 norm={torch.linalg.norm(g_pos_1d).item()}"`. This is the L2-norm identity sanity check — it pins the implementation path as `torch.linalg.norm` reduce, not `abs(g.sum())` or `g.abs().max()` (both would coincidentally return abs(value) on numel=1 but are NOT L2 norm). The assertion uses bare `==` (FP-exact) since on numel=1 tensors `torch.linalg.norm(g).item() == abs(g.item())` in IEEE-754 binary64 (sqrt(g²) is exact for `|g| ≤ 2^52`). Verify by running the test and confirming 6 assertions all PASS.

## 3. Pre-archive lint + test gates

- [x] 3.1 `python scripts/lint_no_source_field_drift.py` exit=0 ✅ (no Source field changes; anchor renumbering doesn't touch Source fields). (no Source field changes; anchor renumbering doesn't touch Source fields). 
- [x] 3.2 `python scripts/lint_no_dead_defensive.py` exit=0 ✅ (no defensive-code changes). (no defensive-code changes; the change is purely text-based).
- [x] 3.3 `uv run pytest tests/test_metrics.py -v` → all tests PASS, `test_cg_n_eq_1_returns_magnitude` with 6 assertions (5 existing + 1 new L2-norm sanity check) all green ✅. → all tests PASS, including `test_cg_n_eq_1_returns_magnitude` with 6 assertions (5 existing + 1 new L2-norm sanity check).
- [x] 3.4 `uv run pytest tests/ -v` → **188 passed, 1 warning** ✅ (full suite, count unchanged from baseline; sanity check is internal to existing test function). → full suite 188 → 188 passed (no count change; sanity check is internal to existing test function).

## 4. Pre-archive validation

- [ ] 4.1 `openspec validate "fix-cg-n-1-test-anchor-collision-and-math-coverage" --strict` → valid: true, issues: [] (no strict errors; spec delta contains exactly one MODIFIED Requirements section with the relabeled anchor `req-35` on "CG n=1 boundary behavior" — same body as before).
- [x] 4.2 Spec delta in `openspec/changes/archive/2026-09-16-fix-cg-n-1-test-anchor-collision-and-math-coverage/specs/wayfinder/spec.md` carries `<a id="req-35">` for CG n=1 boundary ✅ (matches post-apply main spec). in this change directory (`openspec/changes/fix-cg-n-1-test-anchor-collision-and-math-coverage/specs/wayfinder/spec.md`) carries `<a id="req-35">` for "CG n=1 boundary behavior" Requirement (matching the post-apply state).

## 6. Post-archive independent verification (per CLAUDE.md §3 "Post-archive 独立复核" 强制项)

- [ ] 6.1 `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-34"'` returns exactly 1 hit at the Source Field Format Invariant Requirement position (L676+).
- [ ] 6.2 `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-35"'` returns exactly 1 hit at the CG n=1 boundary behavior position (L442+).
- [ ] 6.3 `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'governance-origin requirements trigger lint failure'` returns exactly 1 hit at a line position UNDER the new `req-34` anchor (visually within 5 lines after `id="req-34"`).
- [ ] 6.4 `Select-String -Path openspec\specs\wayfinder\spec.md -Pattern 'id="req-' | Group-Object` returns 35 distinct anchors (`req-1` through `req-35` with the historical gaps at req-6/10/16 unchanged; `req-34` and `req-35` both present once).
- [x] 6.5 `uv run pytest tests/test_metrics.py::test_cg_n_eq_1_returns_magnitude -v` → 1 PASSED (6 assertions: 5 existing + 1 new L2-norm sanity check, no regression) ✅. → 6 assertions all PASS (5 existing + 1 new L2-norm sanity check, no regression).

## 5. Archive

- [x] 5.1 `openspec archive fix-cg-n-1-test-anchor-collision-and-math-coverage --yes` → archived as `2026-09-16-fix-cg-n-1-test-anchor-collision-and-math-coverage`, `specsUpdated: true`, totals: modified=1 ✅. → change 移入 `openspec/changes/archive/2026-09-16-fix-cg-n-1-test-anchor-collision-and-math-coverage/`; sync stage should detect the spec delta (req-35 relabel) and merge into main spec (idempotent since the active spec already has the post-edit state).
- [ ] 5.2 **Pending user commit** — the working-tree edits to `openspec/specs/wayfinder/spec.md` + `tests/test_metrics.py` are NOT yet committed to `dev`. Per CLAUDE.md §4 ("Git 离岸分支管理规范"), commits go through dev (linear) → main (--no-ff) → release (--no-ff + tag). Verification command for after commit: `git log -p dev -- openspec/specs/wayfinder/spec.md | grep -A 3 "id=\"req-35\""` and `git log -p dev -- tests/test_metrics.py | grep "torch.linalg.norm(g_pos_1d)"`.